const initState = {
    item: {},
    totalPrice: 0,
    itemsCount: 0
};

const cart = (state = initState, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
}

export default cart;